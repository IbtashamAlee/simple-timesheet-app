{
  "name": "timesheet-app",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "15.x"
  },
  "scripts": {
    "start": "node ./bin/www",
    "start-dev": "nodemon ./bin/www",
    "heroku-postbuild": "cd client-app && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "mongoose": "^5.12.5",
    "morgan": "~1.9.1",
    "nodemon": "^2.0.7",
    "passport": "^0.4.1",
    "passport-jwt": "^4.0.0"
  },
  "devDependencies": {
    "kill-port": "^1.6.1"
  }
}
